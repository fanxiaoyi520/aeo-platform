---
description: 功能模块实现前按 L1/L2 分级做开源调研；优先依赖或薄封装成熟方案
alwaysApply: true
---

# 开源优先实现（OSS-First，分级）

新能力或非 trivial 重构在写代码前，按任务规模做**分级**开源调研。细则见 `docs/06_TASK_SPEC.md` §开源优先。

## 分级

| 级别 | 触发 | 要求 |
|------|------|------|
| **L0** | bugfix、纯文档/配置、<30min 小改 | 跳过 |
| **L1** | 新脚本、单文件工具、adapter 薄封装 | Spec 中 2～3 行：参考项目 + 采纳方式 |
| **L2** | 新包、新 Agent 能力、外部系统集成（SP-API、队列、风控引擎等） | Top 3 对比表 + 采纳决策 |

## 采纳顺序（L1/L2）

1. **依赖** — `pyproject.toml` / `package.json` 引入维护中的库
2. **薄封装** — OSS 核心 + AEO Adapter（配置、路径、分层边界）
3. **复制代码** — 仅 MIT/Apache-2.0/BSD/ISC；文件头注明来源 URL
4. **自研** — Top 3 均不可用或许可证不兼容；Spec/PR 须写证据

## 禁止

- GPL/AGPL/SSPL 整段复制
- 无 Spec 的大模块从零实现（L2）
- 密钥、未授权店铺数据入库

## 已有选型（无需重复调研）

LangGraph、Chroma、Playwright、FastAPI、Next.js — 沿用即可，除非替换架构。

## 新任务开工流程（L1/L2 强制）

1. **定级**：根据上表判定 L0 / L1 / L2（不确定时按 L2 处理）。
2. **写 Spec**：在对话或 `docs/specs/{任务ID}-*.md` 中附上开源段落（L1 简表 / L2 Top 3 + 采纳决策）。
3. **等「开始」**：用户确认 Spec 后再写代码（与 `06_TASK_SPEC.md` 一致）。
4. **PR**：填写 `.github/pull_request_template.md` §开源调研。

**L2 范例：** [`docs/specs/MV1-07-metrics-sdk.md`](../../docs/specs/MV1-07-metrics-sdk.md)

## 违规与修复

- ❌ L2 新包/新 SDK 无 Top 3 对比即开工
- ❌ 删除、重命名或关闭 `alwaysApply` 的本规则文件
- ✅ 发现规则文件缺失时： `git checkout origin/main -- .cursor/rules/oss-first-implementation.mdc`
