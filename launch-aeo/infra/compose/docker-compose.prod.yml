services:
  postgres:
    image: postgres:16.4
    container_name: aeo-postgres-prod
    environment:
      POSTGRES_USER: aeo
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-aeo_prod_password}
      POSTGRES_DB: aeo
    volumes:
      - pg_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U aeo -d aeo"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - aeo-internal
    deploy:
      resources:
        limits:
          cpus: "1"
          memory: 1G
        reservations:
          memory: 512M

  redis:
    image: redis:7.4
    container_name: aeo-redis-prod
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 3s
      retries: 5
    networks:
      - aeo-internal
    deploy:
      resources:
        limits:
          cpus: "0.5"
          memory: 256M
        reservations:
          memory: 128M

  api:
    build:
      context: ../..
      dockerfile: infra/docker/Dockerfile.api
    container_name: aeo-api-prod
    env_file:
      - ../../.env.prod
    environment:
      APP_ENV: production
      APP_DEBUG: "false"
      DB_URL: postgresql+asyncpg://aeo:${POSTGRES_PASSWORD:-aeo_prod_password}@postgres:5432/aeo
      DB_URL_SYNC: postgresql+psycopg://aeo:${POSTGRES_PASSWORD:-aeo_prod_password}@postgres:5432/aeo
      REDIS_URL: redis://redis:6379/0
    ports:
      - "8000:8000"
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 15s
      timeout: 5s
      retries: 5
      start_period: 20s
    networks:
      - aeo-internal
    deploy:
      resources:
        limits:
          cpus: "2"
          memory: 2G
        reservations:
          memory: 1G

networks:
  aeo-internal:
    driver: bridge

volumes:
  pg_data:
  redis_data:
